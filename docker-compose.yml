version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: tms-backend
    # No external port mapping - Coolify handles this
    expose:
      - "8080"
    environment:
      - PORT=8080
      - DB_PATH=/app/data/tms.db
    volumes:
      - tms-data:/app/data
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - tms-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: tms-frontend
    # No external port mapping - Coolify handles this
    expose:
      - "80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - tms-network

volumes:
  tms-data:
    driver: local

networks:
  tms-network:
    driver: bridge
